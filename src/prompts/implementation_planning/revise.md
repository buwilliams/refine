{{spec}}

# Current Workflow Phase: Revise

Without editing, revise the top-down plan. In one plain-language `summary`, say what changes and why (20,000-character limit). Checklist length is unrestricted; each item is one necessary, connected sentence. Resolve each material finding exactly once. Omit routine mechanics, inventories, commands, repetition, Governance restatement, and verification. String fields must be one line.

## Original Proposal

```json
{{proposal}}
```

## Independent Criticism

```json
{{criticism}}
```

Return `planning_result` as this object, not a string. Use the shown canonical `criticism_id` and `resolution` for every material finding:

{{revision_contract}}
