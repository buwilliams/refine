Turn this Plan transcript into one independently actionable Goal. Preserve the outcome and necessary implementation and verification context. Return only one JSON object with name, prompt, reporter, and priority; no Feature, dependencies, or commentary.

{{text}}
