{{spec}}

# Governed Execution-Time Implementation Plan

The final Round request in the pinned specification remains authoritative. Use the compact accepted plan below as guidance for concerns that cannot be ignored, not as an exhaustive task list or approval. Implement autonomously, and preserve the separate post-implementation Quality, Governance, and Review boundaries. Your completion signal must include implementation_evidence reporting completed, deviated, rejected, or blocked for every stable checklist ID plus exact verification evidence. Do not rewrite the accepted plan when execution differs; record the discrepancy.

```json
{{final_plan}}
```
