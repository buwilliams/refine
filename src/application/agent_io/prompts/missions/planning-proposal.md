Propose a Mission plan of Goal waves for this charter. You see the investigation snapshot and the accepted ledger; the engine validates keys and coverage.

Mission intent: {{intent}}

Success criteria:
{{criteria}}

Accepted knowledge after investigation:
{{ledger}}

Artifact obligations:
{{obligations}}

Return exactly this Mission planning JSON and nothing else:
{{contract}}

Waves are linear; Goals in one wave run in parallel. Give every Goal a stable key, a prompt, criteria it advances, and required vs optional standing. Reuse existing Features through feature_id; never invent dependencies.
