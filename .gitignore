__pycache__/
*.py[cod]
*.egg-info/
.venv/
.env
.DS_Store

# setuptools build artifacts
build/
dist/

# Per-checkout refine binding (links this refine source dir to a target app).
# Created by `refine init`; host-specific; do not commit.
.refine-binding
.refine-apps.json

# Host runner state (pidfile + log) for this clone. Host-specific; do not commit.
/run/
/run.bak*/

# Local investigation artifacts and copied production logs.
/debug/

# Local volume root if anyone tries refine against this repo itself
*.sqlite
*.sqlite-wal
*.sqlite-shm

# Editor state
.vscode/
.idea/
*.swp
